import { Particle } from "tsparticles-engine";
import { OutMode, OutModeAlt, OutModeDirection } from "tsparticles-engine";
import { IBounds, ICoordinates, IDimension } from "tsparticles-engine";
export interface IBounceData {
    particle: Particle;
    outMode: OutMode | OutModeAlt | keyof typeof OutMode;
    direction: OutModeDirection;
    bounds: IBounds;
    canvasSize: IDimension;
    offset: ICoordinates;
    size: number;
}
